# compareWeatherForecasts

Uses OpenWeatherMap's API to compare weather forecasts between two cities. It is done using Python's requests library.

Requires a OpenWeatherMap API Token in compare_forecasts.py
